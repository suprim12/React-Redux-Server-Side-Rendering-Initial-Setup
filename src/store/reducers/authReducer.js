const intialState = {
    isAuthenticated: false,
    user: {},
    loading: false,
}
export const authReducer = (state = intialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}